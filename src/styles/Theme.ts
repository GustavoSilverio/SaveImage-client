import { createTheme } from "@mui/material";

export const theme = createTheme({
	palette: {
		grey: {
			'100': 'rgb(255, 255, 255)',
			'200': 'rgb(28, 28, 28)',
			'300': 'rgb(17, 17, 17)',
			'600': 'rgb(0, 0, 0)',
		}
	}
})